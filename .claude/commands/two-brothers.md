Run a two-solver workflow routing probe.

# /two-brothers

Run `/two-brothers "<problem>"` to execute the two-solver workflow probe and return a valid command result with routing analytics.

## Result

Return `status: success`, a summary, and a workflow probe artifact describing solver count, strategy count, and delivery routing.
