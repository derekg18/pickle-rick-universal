Run a delivery analytics workflow probe.

# /wubba-lubba-dub-dub

Run `/wubba-lubba-dub-dub "<problem>"` to execute delivery analytics for the workflow command group and return a valid command result.

## Result

Return `status: success`, a summary, and delivery analytics tagged as `delivery-analytics`.
