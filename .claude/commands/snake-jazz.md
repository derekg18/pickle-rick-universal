/snake-jazz prepares bounded retry plan artifacts and returns a command result.

Run `/snake-jazz --task "<operation>" --attempts <n>` to emit retry-plan output with bounded attempts, classification steps, and follow-up artifact paths.

The command starts no processes by default. Unsafe execution flags return confirmation follow-up output instead of running retries.
