Prepare a non-destructive downstream safety plan.

Run the shared security/safety helper:

```bash
node "/Users/derekgreene/.gemini/extensions/pickle-rick/extension/bin/security-safety-command.js" interdimensional-customs $ARGUMENTS
```

The command prints a JSON result with `status`, `summary`, `downstream`, and `artifact`. It only returns a downstream contract review plan and does not execute containers, rotate secrets, apply destructive rollback, or invoke syscalls.
