Run a staged workflow routing probe.

# /story-train

Run `/story-train "<problem>"` to execute a staged workflow probe and return a valid command result for command-suite analytics.

## Result

Return `status: success`, a summary, and workflow stages that can be displayed by command surfaces.
